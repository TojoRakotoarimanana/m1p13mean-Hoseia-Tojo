MONGO_URI=mongodb+srv://user:root@mean.b0d13bz.mongodb.net/mean?retryWrites=true&w=majority&appName=Mean

# CORS_ORIGIN specifies which front-end origins are allowed to call this API.
# For local development, only use your local Angular dev server:
CORS_ORIGIN=http://localhost:4200

# For production, set CORS_ORIGIN to your deployed frontend URL, for example:
# CORS_ORIGIN=https://m1p13mean.pages.dev

# If your backend needs to accept requests from multiple origins,
# you can provide a comma-separated list (ensure your CORS setup supports this):
# CORS_ORIGIN=http://localhost:4200,https://m1p13mean.pages.dev
